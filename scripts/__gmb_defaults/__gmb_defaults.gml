///		MACROS


///		ENUMS
enum ITEMTABLE {
	NAME,		// 0	Name		\	Defaults, can be changed but correlate to the columns in the item_table Class,
	DESC,		// 1	Description	 | 	it's recommended you have an enum count the same as the width of the item_table
	SPR			// 2	Sprite		/   when you initiate the class.
}